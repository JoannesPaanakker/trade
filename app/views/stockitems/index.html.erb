<h1>Stock items</h1>
<h4>For trade</h4>
</div>
All stockitmes for sale:
<% @stockitems.each do |s| %>
  <div class="challenge">
    <%= s.catalogitem.description %>
    <%= s.catalogitem.brand %>
    <%= s.catalogitem.product_code %>
    € <%= s.sell_price %>
    <%= @region %>
    <%= @shoesizes[s.internal_size] %>
    <button class="knop-1"><%= link_to "details", stockitem_path(s) %></button>
  </div>
<% end %>

<br />
<button class="knop-1"><%= link_to "new stockitem", new_stockitem_path %></button>
